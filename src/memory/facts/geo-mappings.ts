/**
 * @deprecated 地理解析域已迁至 `@resolution/geo`（geo-domain-refactor-plan v3.1 Phase 1），
 * 请从 `@resolution/geo` 导入。本门面全量 re-export 旧路径的全部符号
 * （数据表/函数/类型），保留至少一个发布周期，Phase 5 收口删除。
 * 新代码禁止 import 本路径（ESLint no-restricted-imports 固化，§12）。
 */
export * from '@resolution/geo';
