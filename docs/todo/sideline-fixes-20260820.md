# 收资批随批修复项（0820 裁定，域外三件）

> 来源：0820 两周 BadCase 全量审判（58 条待分析）中的域外发现，用户裁定前两件
> **随收资表单批顺带修复**，第三件产品口径已定、落话术层。
> 与收资状态机蓝图（collection-form-machine-implementation.md）同批执行，但代码互不耦合。

## 1. 内部独白/推理产物泄漏——守卫 sanitizer 缺口（4 例 / 3 会话，8 月新簇）

生产证据：
- `nkyiuvdx`（chat 6a7c077c…）：直发 `</antThinking>` 标记；
- `z3toosp6`（chat 6a7edda9…）：直发英文推理独白 "Now confirmed 0 results twice. Proceeding with the script and group invite"；
- `dnjmwffe`/`lrerr7zx`（chat 6a82ab69…）：直发"我应该简洁地回答这两个问题""根据工具查询结果…"中文自我指令 + 内容复读。

定性：真人插话旁白泄漏案（2026-07）当时定为 30 天孤例，**现已成簇**——模型降级时把
推理/自我指令混进投递文本，出站守卫对三种形态（XML 残标、英文独白、中文元叙述）
拦截不全。修法方向（沿用 7-15 旁白案深挖结论）：**主修 hard-rules 的
internal_output_leak 加 pattern 族**——它是唯一必跑档、能覆盖零工具轮（语义评审
对零工具轮结构性失明）；sanitizer 确定性剥离为辅（对齐 fence 剥离先例）；拦截/剥离
样本落 guardrail_review_records；**禁用 LLM 实时判断**（实时反馈循环禁令仍适用）。
注意 7-10 守卫大下线裁定仍在：新增 pattern 须逐条挂本簇 badcase 实证，勿顺手重建
已删规则。

## 2. 静默丢消息——7-29「暂不修」裁定推翻（0820 用户裁定：本批修）

生产证据：`qhjyajuq`（chat 6a7c265b…）——16:43 说"帮你查下临平路附近的岗位"后整晚
无下文，次日真人道歉补救，运营标"严重影响转化"。

背景：静默窗口+锁重检两漏口（project_silent_message_drop_quiet_window_lock），
7-29 裁定暂不修，0820 携新伤亡推翻。修法按已诊断的两漏口收口；验收=同形态
（承诺查询后整轮无响应）在观测上可发现、不再静默。

## 3. 无岗承接口径（0820 产品裁定，落话术/strategy 层，收敛三条互斥运营诉求）

1. **推荐上限 2 轮**：同一候选人对推荐结果连续 2 轮不满意，停止继续推荐，转入拉群承接
   （源诉求 `lad4asid`）；
2. **真无岗不拉群**：搜索确实无岗（如暑假工无库存）时，暂时不拉群（源诉求 `g4awefcm`）；
3. **拉群即收口**：决定拉群后不再输出重复的推荐/追问话术（"要不要看其他区域"类），
   直接发邀请卡片+群介绍即可（源诉求 `skgxdafo`）。

既有裁定衔接：拉群两轮协议（征询→同意→实调 invite）与守卫"只拦已拉假宣称"不变；
本口径约束的是**推荐轮次上限**与**拉群决策后的话术**，不改邀请协议本身。
