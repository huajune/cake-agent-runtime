# BadCase 未解决积压待办（飞书 BadCase 表）

- **所有者**：用户裁定 + 运营核对；工程项由 Claude 执行
- **数据源**：飞书 BadCase 表。逐条结论以飞书「修复说明」为准，本文只列仍需动作的簇，不复制逐条状态
- **完成条件**：下列每一簇转工程实现 / 运营核对 / 暂搁置并回写飞书后，删除本文

术语：当前 Output 单次审查为 pass / observe / repair / replan，旧 revise / block 对应 repair；口径见 [守卫治理体系](../architecture/guardrail-quality-system.md)。

## 一、需要用户 / 产品裁定

### B. 薪资 / 福利口径配置化（9 条）

`aftkl8jf` `ea6i2wh0` `qnpeyejd` `hxx5xnd8` `ali3daia` `crtum1c7` `344if90q` `i3lz12lb` 及同源项。
运营诉求彼此冲突：「六姐说时薪就行」「别说综合薪资」「范围薪资高的可以说范围」「必胜客说公司提供保险易被误解成社保」「不主动提薪资容易变成自招」。当前薪资展示是全局统一口径，无法同时满足。

- [ ] 产品先定义口径枚举（时薪优先 / 综合薪资优先 / 区间展示 / 必须主动提）
- [ ] 下沉成品牌 / 项目级配置（`hosting_config` 或岗位渲染的品牌开关），运营维护「项目 → 薪资展示口径」表。不再往 prompt 里堆规则

### G. 产品级流程裁定（7 条）

- [ ] `9m5exulb` 换店报名后旧门店工单未取消：是否做成「换店即自动取消旧工单」硬编排（当前已做到当轮问清是否取消，未自动取消）
- [ ] `gh137zjo` AI 面试未完成不得引导到店：需要产品明确「线上面试不可跳过」的判定源（岗位字段？）
- [ ] `mjv4bhn3` 面试地址非本门店（新店未开业在老店面试）时应主动说明原因：需要数据侧提供「面试门店 ≠ 上岗门店」字段
- [ ] `6o2qyw5i` 撤回消息应让 AI 感知：需渠道侧确认企微回调是否下发撤回事件
- [ ] `hxaec4s1` `1dre4xlf` 学生身份改口：现行「被拒后改口须核实一次」，运营认为不该引导改口。是否收紧成「学生身份一旦确立即锁定」
- [ ] `plxhjn0c` 候选人填写不清时应追问而非放行
- [ ] `crtum1c7` 是否强制主动播报薪资（与 B 组同源）

### E1. 收资契约同义标签重复（`9xzdlrua`）

同一张表同时出现「健康证」与「有无本地健康证」两格，候选人被问两遍。代码侧按 `labelTitle` 原文 100% 直出的既有裁定不能合并。

- [ ] 运营在海绵侧合并标签；若要代码兜底，需先撤销上述裁定

## 二、工程待实现

- [ ] **排班承诺 vs 岗位出勤要求的执行档对账**（`p43zzrtc` `lm28v8e6` `l926fy0l` `cy91qtkl`）：本轮回复中的排班数字与会话累计工具证据里该岗位的结构化排班字段不一致、且本轮零查岗工具 → repair，反馈「引用在案数据纠正或先调工具核实」。判据是数字对账不是道歉词面。observe 方案已被用户否决，直接上对账
- [ ] **「复述候选人说过的话须有原文」哨兵**（`5abz1b8m`）：臆造「候选人说每周上三天」这类归因
- [ ] **海绵组合班次字段语义确认**（`8j7btoh7`）：「每天两个班次」是轮班不是连上 14 小时。确认后可把现行「排班数据冲突提示」升级为合并语句
- [ ] `vaucmdx1` 人民广场类跨城歧义地名先问候选人城市：核对是否已被 geo 歧义策略覆盖，未覆盖则登记

## 三、运营核对

- [ ] **C. 岗位数据真实性**（暂搁置 23 条）：「根本没这岗位」「有包吃住的岗位没推荐」「哪有工作餐」等。岗位滚动上下架，案发证据保质期仅数天，运营按 case 逐条实时探针海绵岗位库字段；本轮不改代码也不据此改渲染

## 四、观察中（单例不成簇，复现再动）

- I 组输出形态：`xgwyjn3t` 繁体字、`lkjcvyqg` 开局自我介绍名字、`z3toosp6` 表情消息报错、`v86kvecw` 二次触达纠纷
- `r3tv7205` 昵称等于普通短语时验证语被误读
- F 组时延（`7exhawrl` `1zjtlr2l`）：`llm_execution` 尝试轨迹埋点已随 v11.1.0 上线两周，可按 `attemptCount>1` 下钻归因，再定是给 provider 出站加统一超时还是换模型档位
- 管道观察项（自判官标定周任务移入）：复聊发送与候选人来消息同分钟撞车 3 例；`interview_time_change_unconfirmed` 两次把改约回复整条拦成静默，交守卫周分析复核真假阳；入职跟进缺「真人已问过」停止条件

## 五、维持「处理中」、逐条结论在飞书

`ypzu02ai` 报名成功未播报、`0zyiwkf3` 不需简历的岗位误传简历、`kseb7m68` 换店报名却未推荐过该门店、`cy91qtkl` 体重判定。无成簇证据，逐条下钻，不在本文重复状态。

## 已定案，不再计入

- A1 岗位侧排班渲染歧义、A2 候选人侧「做 N 休 M」口语解析：已修
- D 组召回策略 9 条：已逐条定案，简名重试与零工具宣称查岗的守卫已上线
- H 组昵称识别成品牌、乐高乐园简名、裸两位数按年龄、性别自陈对照岗位：已修
- 8/31 与 9/8 两轮复核共 100 条转已解决、2 条转暂搁置，依据均已写入飞书「修复说明」
