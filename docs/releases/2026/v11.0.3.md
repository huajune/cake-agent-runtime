# 发版底账：recap 确认词表扩词 + booking 失败轮守卫收窄

## 1. 发布身份

| 字段       | 内容 |
| ---------- | ---- |
| 状态       | 已发布 |
| 计划版本   | v11.0.3 |
| 发布日期   | 2026-08-27 |
| Release PR | #1074 |
| 实现 PR    | #1072（发版链路修补：#1075 解元数据冲突、#1078 回同步） |
| 目标环境   | 生产 |
| 负责人     | jiezhu |

## 2. 范围与风险

### 变更摘要

- recap 确认词表扩词（`resolution/signal/dialogue.ts`）：`RECAP_ONLY_TOKEN_SOURCE` 补入 行/好/中/妥/要得/得嘞/没毛病/阔以/OK/嗯呐/约吧 等整句放行表态，仅进 recap 组合通道，不进身份作证词表。生产复现：候选人对提交前复述回「行」，词表无此词导致确认整轮退化、booking 被凭据闸拒绝（batch …_1787812777667），与 0819「确定」一字之缺同病。
- `booking_receipt_mismatch` 失败路径收窄（用户裁定）：删除 `BOOKING_RETRY_PROMISE_PATTERN` 整段——booking 失败后"稍后再帮你提交"是合法承接话术，与拉群守卫"只拦完成时态"同口径；in-flight 假宣称只保留「正在」词形。继续拦：宣称正在/已经提交的假回执。
- 规则台账同批登记（`docs/prompt-rule-ledger.md`）。

### 高风险区域

- P0：扩词若误收歧义词，疑问/否定短答可能被判确认，导致未经候选人放行的预约提交。
- P0：守卫收窄后，假宣称"已/正在提交"的拦截不得回退。
- P1：成功路径既有形态（E 未播报日期 / F 在途工单改时间）不得受影响。

### 数据库、配置与外部依赖

| 类别                   | 变化 | 发布前动作 | 兼容/回滚说明 |
| ---------------------- | ---- | ---------- | ------------- |
| Migration / RPC / 权限 | N/A  | N/A        | 无 schema 或数据迁移 |
| 环境变量 / 运行时配置  | N/A  | N/A        | 无配置变化 |
| 外部服务 / 权限        | N/A  | N/A        | 无外部接口契约变化 |

### 部署顺序与回滚

- 部署顺序：应用镜像单步更新，无前置 migration/config 动作。
- 回滚版本：`v11.0.2`。
- 回滚条件：疑问/否定短答被误判确认导致误提交、失败轮假回执漏拦成簇，或健康检查失败。
- 回滚动作：使用现有部署工作流恢复 `v11.0.2` 镜像；词表与守卫均为无状态纯函数，无数据兼容问题。

## 3. 回归策展

### P0：发布阻断

| ID    | 场景与输入 | 期望结果 | 验证方式 | 状态 | 证据 |
| ----- | ---------- | -------- | -------- | ---- | ---- |
| P0-01 | 候选人对提交前复述回「行」及扩展词（好/中/妥/要得/OK/约吧…，生产等价输入） | 判定为复述确认，放行 ready_to_book | 单元测试 | 通过 | `dialogue.spec.ts` 扩展词正例 22 条 |
| P0-02 | 疑问/否定/歧义形态（行吗/行不行/好了/不行/别提交了/先别约吧/中午） | 不判确认 | 单元测试 | 通过 | `dialogue.spec.ts` 负例 8 条 |
| P0-03 | booking 失败 + 回复宣称"已经/正在提交""那就约在" | 仍 REVISE 拦截 | 规则级测试 | 通过 | `booking-receipt.rule.spec.ts` 失败路径用例 |
| P0-04 | booking 失败 + 回复如实披露并承诺"稍后再帮你提交" | 放行，不再拦截 | 规则级测试 | 通过 | `booking-receipt.rule.spec.ts` 失败路径用例 |
| P0-05 | 身份作证通道（isAffirmativeAnswer）不随 recap 扩词扩散 | 「可以/行」等仍不命中单短答作证 | 单元测试 | 通过 | `dialogue.spec.ts` 作证词表隔离用例 |

### P1：重点观察

| ID    | 场景与输入 | 期望结果 | 验证方式 | 状态 | 证据 |
| ----- | ---------- | -------- | -------- | ---- | ---- |
| P1-01 | 成功路径形态 E（未播报日期）/ F（在途工单未改约） | 行为不变 | 规则级测试 | 通过 | `booking-receipt.rule.spec.ts` 既有用例全绿 |
| P1-02 | 失败轮回复裸「没约上」如实披露 | 不被 CONFIRMATION_PATTERN 的「约上」误拦 | 规则级测试 | 通过 | 修复与用例随本底账 PR 收编 develop，随下一版本发布 |

## 4. 验证记录

### 自动化检查

| 检查               | 结果 | 证据 |
| ------------------ | ---- | ---- |
| 定向测试           | 通过 | 发版前 4 suites / 93 tests；底账补充 2 suites / 38 tests |
| lint / typecheck   | 通过 | `pnpm run lint:check`、`pnpm run typecheck` 本地全绿 |
| 全量测试 / CI      | 通过 | pre-push 全量 `ci:check`；远端 CI Checks + ai-code-review 全绿 |
| `git diff --check` | 通过 | 本地检查无输出 |

### 模型、人工与外部联调

| 验证项         | 结果 | 证据 / 说明 |
| -------------- | ---- | ----------- |
| 模型回放       | N/A | 未修改 prompt 或模型决策；词表与守卫均为确定性代码，生产等价输入走单元/规则级测试 |
| Dashboard / UI | N/A | 无 Dashboard/UI 变化 |
| 外部服务联调   | N/A | 无外部接口契约变化 |
| 数据库状态     | N/A | 无 migration/schema/RPC 变化 |

## 5. 发布闸口

- [x] P0 全部通过，P1 无新增阻断性回归
- [x] required checks 全绿，阻塞 review 已解决
- [x] 配置、权限、migration 和外部依赖已确认
- [x] 部署顺序、回滚版本、回滚条件与负责人明确
- [x] 版本、CHANGELOG、Release PR 和底账一致

## 6. 发布结果

| 字段           | 内容 |
| -------------- | ---- |
| 最终版本 / tag | v11.0.3（tag 指向固化 commit 72a67746） |
| Release URL    | https://github.com/huajune/cake-agent-runtime/pull/1074 |
| 部署 workflow  | Build and Deploy run 33050781681（test / deploy / notify 全部 success） |
| 实际发布时间   | 2026-08-27 07:41 UTC（北京时间 15:41） |
| 生产验证       | 部署工作流内建健康检查通过；发布后抽查消息处理流水正常产出 |
| 监控与告警     | 观察 `guardrail_review_records` 中 booking_receipt_mismatch 命中应显著下降；`collection_form_audit` 中复述确认卡壳（confirm_collection 重复轮）应下降 |
| 回滚情况       | 未发生 |

## 7. 遗留事项与修订记录

- 遗留事项：
  - 发布后观测：候选人短答确认场景的 ready_to_book 放行率、booking.rejected（凭据拒绝）次数应下降；关注扩词是否引入误判确认（预期为零，规则形态上疑问/否定不可命中）。
  - 「没约上」如实披露误拦修复（BOOKING_FAILURE_ACKNOWLEDGED_PATTERN 补裸词形）已随本底账 PR 入 develop，随下一版本发布。
  - recap 确认根治（模型作证 + 确定性公证通道，替代词表打地鼠）待立项。
- 修订记录：2026-08-27 发布完成后补记（本版发版 PR 合入时底账校验曾报缺失，属流程疏漏，随即补齐）。
