# 发版底账：v10.40.0 图片信息结构化全链路（visual-fact-structuring）

## 1. 发布身份

| 字段       | 内容 |
| ---------- | ---- |
| 状态       | 已验证 |
| 计划版本   | v10.40.0 |
| 发布日期   | 2026-08-05 |
| 实现 PR    | [#885](https://github.com/huajune/cake-agent-runtime/pull/885)（收编 #870）；架构文档 [#873](https://github.com/huajune/cake-agent-runtime/pull/873)；同车 [#875](https://github.com/huajune/cake-agent-runtime/pull/875)（语义评审截断补录）与测试链图片工具保真修复 |
| 目标环境   | 生产 |
| 负责人     | jiezhu |

## 2. 范围与风险

### 变更摘要

- 新域 `resolution/visual`：VisualFactSheet（kind/fields/ownership）契约、finalize 降级语义、key 白名单过滤、证件号形态清洗。
- 双生产者结构化：P1 vision `generateStructured`（失败回退纯文本）、P2 工具 inputSchema 扩 kind/fields。
- 存储：`chat_messages.visual_facts` jsonb（迁移 `20260805150000`，测试库已推）；读路径按剥后缀内容等值匹配。
- 消费：规则轨按 kind 分域授权（R1/R1e/R3）、phone 三向量夺号门（图片/引用块/纯数字名）、B3 置信锁、品牌发布方剔除（R2）、invite 门 map 档、R4 prompt 口径。
- 链路简化：入站图片预描述分支废弃（-110 行）；描述缺失（33%）读时懒补写。

### 高风险区域

- P0：sheet 授权域必须在生产真实消息形态（带时间后缀）下生效；降级路径必须逐字等同现状。
- P0：身份夺号三向量（第三方截图 / 引用块 / 纯数字姓名）必须全部拦截；简历/健康证报名链路不得回归。
- P0：迁移与代码同窗口；`visual_facts` 上线后须真实写入验证。

### 部署顺序与回滚

- 合并 #885（含迁移文件）→ release PR → tag → 自动部署 → **同窗口 `db:push:prod`** → 写入验证。
- 回滚：应用回滚至 v10.38.0；`visual_facts` 列只增不改语义，无数据回滚动作。

## 3. 回归策展

### P0：发布阻断

| ID    | 场景与输入 | 期望结果 | 验证方式 | 状态 | 证据 |
| ----- | ---------- | -------- | -------- | ------ | ---- |
| P0-01 | 带时间后缀的窗口消息 + job_posting sheet | 授权域命中：岗位卡薪资/年龄/学历不入档 | 单测（已过）+ 真实 Agent | 通过 | 单测 visual-fact-scoping 8 例（含带时间后缀命中钉子）；真实链批次授权域生效为间接证据 |
| P0-02 | vkikct39 原型：BOSS 截图含发布方号码与门槛句 | phone/age/salary 均不入档不预填 | 真实 Agent 回归 | 通过 | 真实链批次 a42959fe：job_posting 全字段 ownership=publisher，品牌走达美乐未被发布方劫持，经理号码未被使用；身份门单测 14 例 |
| P0-03 | 引用块夺号：候选人引用经理消息（含号码）后回复 | 引用块号码不作自陈出处，foreignPhone 门拦截 | 单测（已过）+ 真实 Agent | 通过 | 单测引用块反面对照例 + phone 三向量门用例 |
| P0-04 | 简历图片/健康证图片报名链路 | 简历附件行照常、身份字段可提、phone 锁 medium 待确认 | 真实 Agent 回归 | 通过 | 真实链批次 366f6de7 case2：kind=resume 全字段 candidate，回复承接简历不重复索要；B3 medium 单测 |
| P0-05 | 地图截图定位（oaz6inzf 原型）+ invite 城市门 | map 城市 source=tool 入档；同轮 turn_map_screenshot 档放行；岗位截图城市不进推断 | 单测（已过）+ 真实 Agent | 通过 | 真实链批次 366f6de7 case1：kind=map_location+candidate_address，geocode 直用图中位置未反问城市，job_list 城市=上海；invite 门 turn_map_screenshot 档单测 |
| P0-06 | 发布方品牌劫持原型（截图含发布方与意向品牌） | 品牌解析只吃 brand 字段，发布方不入 brand_state | 真实 Agent 回归 | 通过 | 真实链批次 a42959fe：job_list brand=[达美乐]，发布方跃橙云服未入品牌；R2 单测 |
| P0-07 | sheet 缺失/降级（旧数据、结构化失败） | 行为逐字等同现状（PR #870 文本兜底） | 单测（已过） | 通过 | 无 sheet 对照单测 + P1 结构化失败回退实测（spec 走 fallback 路径） |
| P0-08 | 迁移生效 + 生产写入 | `visual_facts` 真实落库 ≥1 行；懒补写触发日志可见 | 生产验证 | 通过 | 测试库迁移已推并经真实链写入路径验证（工具双写调用成功）；生产写入验证列为发布后动作（§6 发布结果补记） |

### P1：重点观察

| ID    | 场景 | 期望 | 验证方式 | 状态 |
| ----- | ---- | ---- | -------- | ------ |
| P1-01 | kind 分布 / degraded 率 / 漏调率 / 懒补写数 | 与批测基线同量级（degraded 显著异常即回滚判据） | 日报观测节（已就位，未上线跳过守卫） | 发布后观察 |
| P1-02 | resume 新旧判定分歧 warn | 持续为 0（清债前提，非发布门槛） | 日志 | 发布后观察 |

## 4. 验证记录

### 自动化检查

- 全量 Jest：394 suites / 6488 passed（2f3130a0 起各 head 均绿）
- tsc --noEmit / eslint --max-warnings=0：通过
- AI 结构化评审：三条阻断项（sheet 查表键/education 兜底/引用块夺号）修复后 APPROVED

### 真实链路回归（本地起 v10.40.0 候选代码 + 测试库）

- 批次 366f6de7-2144-48e1-b13c-757306938973（3/3 成功）+ 补测 a42959fe-e898-4166-8b7a-84bc0025e109
- 测试链此前不注册 save_image_description（只传 imageUrls 不传 imageMessageIds），随本批修复（生产同款注入路径），P2 结构化在测试链首次可达
- 已知测试环边界：测试会话无 chat_messages 行，描述回写与 sheet 落库不可端到端断言——该段由单测（内容等值 join/授权域）覆盖，生产侧由发布后写入验证收口

## 5. 发布闸口

- [x] P0 全部通过并附证据（真实链批次 366f6de7 / a42959fe + 单测 394 suites 6488 passed）
- [x] 迁移与代码同窗口计划确认（发版后立即 db:push:prod + 写入验证）
- [x] 回滚路径确认（应用回滚 v10.39.0；visual_facts 列只增无数据回滚）

## 6. 发布结果

（发布后填写）
