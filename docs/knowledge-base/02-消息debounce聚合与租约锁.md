---
tags: [消息队列, 分布式锁, redis, bull, 学习]
source: src/channels/wecom/message/runtime/simple-merge.service.ts
---

# 消息 Debounce 聚合与租约锁（SimpleMerge）

## 解决什么问题

微信用户打字习惯是把一句话拆成 3~5 条短消息连发（"你好" / "我想找工作" / "在杭州"）。如果每条消息都触发一次 Agent 回合：贵、慢、回复互相打架。需要把一轮"连发"聚合成一次 Agent 调用。

## 为什么用 debounce 而不是固定窗口

固定窗口（收到第一条后等 N 秒）有个死缺陷：用户第 N-1 秒还在打字，窗口一关就把半句话送进 Agent。**debounce 语义**是"距离**最后一条**消息静默满 N 秒才处理"——只要用户还在连发，窗口就一直顺延。

## 实现（Redis List + Bull 延迟任务）

1. 每条消息 RPUSH 进 `pending` Redis List（TTL 300s 兜底）
2. **每条消息都**注册一个 Bull 延迟任务，`delay = 静默窗口`（窗口值存 `hosting_config` 表，Dashboard 可调，不用发版）
3. Worker 触发时检查"距最后一条消息是否已静默足够久"：
   - 不够久（说明后面又来了新消息）→ 直接退出，交给更晚的那个任务
   - 够久 → 取出整个 List，合并为一轮，调 Agent

## 租约锁三时长模型（这是学习重点）

Agent 一轮要跑几十秒到几分钟，处理期间必须防止并发 worker 重复消费。用 Redis 锁，但**固定长 TTL 和无 TTL 都是错的**——发版重启杀死持锁进程后锁会孤悬。最终是三个时长互相约束：

| 常量 | 值 | 约束关系 |
|---|---|---|
| PROCESSING_LOCK_TTL | 90s | 单次租约。崩溃后孤悬锁最长活 90s，远小于 pending 的 300s |
| LOCK_HEARTBEAT_INTERVAL | 30s | 心跳续期，必须 < TTL/2，每个租约至少 2 次续期机会 |
| LOCK_RETRY_DELAY | 30s | 锁冲突时**补建重检任务**的延迟，孤悬锁最长 120s 内被新 worker 接手 |

## 踩坑史：2026-06-09 v5.13.0 发版丢消息事故

早期实现里锁冲突时 worker **静默跳过**（假设"下一条消息会再建任务"）。但如果用户不再发消息：持锁进程被发版 SIGTERM 杀死 → 锁孤悬到 TTL → 期间所有检查任务都跳过了 → pending List 等到自己 300s TTL 过期 → **消息永久丢失，无任何报错**。修复：锁冲突时不再裸跳过，而是给 pending 续期 + 按 30s 桶补建重检任务（桶化防止重检任务风暴）。

队列任务创建本身也有本地重试（200ms * attempt 退避），失败上抛而非静默丢——原则是**宁可报错也不静默丢消息**。

## 关联

- 发版重启丢在途消息的更大话题见 [[14-生产事故复盘集]]
- 处理完成后的回合流程见 [[06-Agent回合编排与TurnFinalizer]]
